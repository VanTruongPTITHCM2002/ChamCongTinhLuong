
import { Metadata } from "next";
export const metadata: Metadata = {
    title: "Quản lý tính lương",
    description: "Generated by create next app",
  };
  
  export default function AdminPayrollLayout({
    children,
  }: Readonly<{
    children: React.ReactNode;
  }>) {
    return (
        <div>
          {children}
          </div>
    );
  }